{
  "generated_by": "short_video_engine",
  "contract_id": "semantic_motion_contract_v2",
  "semantic_actions": {
    "negate_and_redefine": {
      "required_slots": ["subject", "rejected_a", "rejected_b", "accepted_definition"],
      "required_visual_actions": ["show_rejected_a", "reject_a", "show_rejected_b", "reject_b", "reveal_definition", "show_connection_flow"],
      "recommended_template": "negation_to_connector_scene"
    },
    "connector_metaphor": {
      "required_slots": ["input", "connector", "output"],
      "required_visual_actions": ["input_flow", "through_connector", "output_flow"],
      "recommended_template": "connector_flow_scene"
    },
    "metric_growth": {
      "required_slots": ["metric", "baseline", "target_or_delta"],
      "required_visual_actions": ["reveal_baseline", "draw_growth_curve", "reveal_delta", "direction_emphasis"],
      "recommended_template": "narrative_trend_curve_scene"
    },
    "process_migration": {
      "required_slots": ["old_step", "new_step", "result"],
      "required_visual_actions": ["old_path", "transition", "new_path"],
      "recommended_template": "process_migration_scene"
    },
    "density_comparison": {
      "required_slots": ["old_solution", "new_requirement", "new_solution"],
      "required_visual_actions": ["old_limit", "requirement_pressure", "new_solution_expansion"],
      "recommended_template": "density_pressure_scene"
    },
    "concept_definition": {
      "required_slots": ["subject", "definition", "role"],
      "required_visual_actions": ["show_subject", "show_definition", "show_role_connection"],
      "recommended_template": "concept_definition_scene"
    },
    "cause_to_result": {
      "required_slots": ["cause", "mechanism", "result"],
      "required_visual_actions": ["show_cause", "show_mechanism", "show_result"],
      "recommended_template": "progressive_relation_graph_scene"
    },
    "before_after_change": {
      "required_slots": ["before", "transition", "after"],
      "required_visual_actions": ["show_before", "transform", "show_after"],
      "recommended_template": "before_after_scene"
    },
    "relation_network": {
      "required_slots": ["core", "dependency_a", "dependency_b"],
      "required_visual_actions": ["show_core", "grow_relation_a", "reveal_dependency_a", "grow_relation_b", "reveal_dependency_b", "settle_network"],
      "recommended_template": "progressive_relation_graph_scene"
    },
    "trend_timeline": {
      "required_slots": ["metric", "start_period", "pivot_period", "end_period", "trend_label"],
      "required_visual_actions": ["reveal_axis", "draw_trend", "reveal_pivot", "extend_projection", "hold_outlook"],
      "recommended_template": "narrative_trend_curve_scene"
    },
    "bottleneck_evidence": {
      "required_slots": ["subject", "bottleneck", "duration_or_metric"],
      "required_visual_actions": ["locate_subject", "pin_bottleneck", "reveal_metric", "hold_evidence"],
      "recommended_template": "evidence_callout_overlay_scene"
    }
  }
}
